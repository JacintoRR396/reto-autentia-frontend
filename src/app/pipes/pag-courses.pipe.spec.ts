import { PagCoursesPipe } from './pag-courses.pipe';

describe('PagCoursesPipe', () => {
  it('create an instance', () => {
    const pipe = new PagCoursesPipe();
    expect(pipe).toBeTruthy();
  });
});
